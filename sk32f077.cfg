# SK32F077: register-compatible with STM32F072, but the F_SIZE register
# reads garbage in upper bits. Force the flash bank size to 64 KB so the
# stm32f1x driver does not probe a wrong value.
set FLASH_SIZE 0x10000
source [find target/stm32f0x.cfg]
